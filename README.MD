# Getting started
Install `uv`: `pip install uv`,

Run: `uv sync --all-groups` to create venv, run `.venv/scripts/activate` for linting in editor, configure interpreter to `.venv` folder (select `python.exe`).